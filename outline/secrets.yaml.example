# Outline Configuration
# Copy to secrets.yaml and encrypt with: sops -e -i secrets.yaml

outline_url: https://wiki.xdeca.com

# Generate with: openssl rand -hex 32
secret_key:
utils_secret:

# Postgres - generate with: openssl rand -hex 16
postgres_password:

# MinIO (S3-compatible storage)
minio_root_user: outline
minio_root_password:   # generate with: openssl rand -hex 16
minio_url: https://storage.xdeca.com

# SMTP (Brevo or other provider)
smtp_host: smtp-relay.brevo.com
smtp_port: "587"
smtp_username:
smtp_password:
smtp_from_email: noreply@xdeca.com
smtp_secure: "false"
