# Copy to Caddyfile and update domains
# Caddy automatically handles HTTPS via Let's Encrypt

openproject.example.com {
    reverse_proxy localhost:8080
}

twenty.example.com {
    reverse_proxy localhost:3000
}
