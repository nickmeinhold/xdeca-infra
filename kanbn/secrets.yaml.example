# Kan.bn secrets (encrypt with: sops -e -i secrets.yaml)

kanbn_url: https://tasks.xdeca.com

# Generate with: openssl rand -hex 32
auth_secret:

# Postgres - generate with: openssl rand -hex 16
postgres_password:

# SMTP (Brevo or other provider)
smtp_host: smtp-relay.brevo.com
smtp_port: "587"
smtp_username:
smtp_password:
smtp_from_email: noreply@xdeca.com

# Trello import (optional)
trello_api_key:
trello_api_secret:

# S3-compatible storage (MinIO)
s3_endpoint: https://storage.xdeca.com
s3_access_key_id:
s3_secret_access_key:
next_public_storage_url: https://storage.xdeca.com

# Webhooks (optional)
webhook_url:
webhook_secret:
